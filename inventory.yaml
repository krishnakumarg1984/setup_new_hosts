---
all:
  children:
    arm64_machines:
      hosts:
        ucl_locust_ssh_gateway:
        ucl_cricket_ssh_gateway:
    x86_64_machines:
      hosts:
        blend:
        ucl_terrarium_ssh_gateway:
        ucl_mandelbrot_ssh_gateway:
    remote_machines:
      hosts:
        ucl_locust_ssh_gateway:
        ucl_cricket_ssh_gateway:
        ucl_terrarium_ssh_gateway:
        ucl_mandelbrot_ssh_gateway:
    arm64_remote_machines:
      hosts:
        ucl_locust_ssh_gateway:
        ucl_cricket_ssh_gateway:
    x86_64_remote_machines:
      hosts:
        ucl_terrarium_ssh_gateway:
        ucl_mandelbrot_ssh_gateway:
    remote_linux_machines:
      hosts:
        ucl_locust_ssh_gateway:
        ucl_cricket_ssh_gateway:
        ucl_terrarium_ssh_gateway:
        ucl_mandelbrot_ssh_gateway:
    arm64_remote_linux_machines:
      hosts:
        ucl_locust_ssh_gateway:
        ucl_cricket_ssh_gateway:
    x86_64_remote_linux_machines:
      hosts:
        ucl_terrarium_ssh_gateway:
        ucl_mandelbrot_ssh_gateway:
    local_machines:
      hosts:
        blend:
    local_linux_machines:
      hosts:
        blend:
    x86_64_local_linux_machines:
      hosts:
        blend:
    x86_64_local_machines:
      hosts:
        blend:
