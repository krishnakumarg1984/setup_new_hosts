---
all:
  children:
    remote_hosts:
      children:
        remote_linux_hosts:
          children:
            arm64_remote_linux_hosts:
              hosts:
                ucl_locust_ssh_gateway:
                ucl_cricket_ssh_gateway:
            x86_64_remote_linux_hosts:
              hosts:
                ucl_terrarium_ssh_gateway:
                ucl_mandelbrot_ssh_gateway:
    local_hosts:
      children:
        local_linux_hosts:
          children:
            x86_64_local_linux_hosts:
              hosts:
                blend:
